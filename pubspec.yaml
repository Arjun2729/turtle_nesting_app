name: olive
description: A Flutter project for turtle nesting data collection

publish_to: none
version: 1.0.0+1

environment:
  sdk: ">=3.6.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  google_sign_in: ^6.1.1
  gsheets: ^0.5.0
  geolocator: ^9.0.2
  google_maps_flutter: ^2.2.5
  intl: ^0.18.0
  shared_preferences: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/service_account.json
